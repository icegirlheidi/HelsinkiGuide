<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionBarTheme">@style/HelsinkiGuideAppBarTheme</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Style for a category of vocabulary words -->
    <style name="CategoryTab" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="tabIndicatorColor">@color/colorAccent</item>
        <item name="tabBackground">@color/colorPrimary</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/colorSecondaryText</item>
    </style>

    <!-- App bar theme -->
    <style name="HelsinkiGuideAppBarTheme" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="colorControlNormal">@color/colorPrimaryText</item>
    </style>

    <!-- Contact and More info style for place details list item -->
    <style name="contact_and_more_info_list_style">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:paddingLeft">@dimen/place_details_list_item_margin</item>
        <item name="android:paddingRight">@dimen/place_details_list_item_margin</item>
        <item name="android:paddingTop">@dimen/place_details_list_item_margin</item>
        <item name="android:drawablePadding">@dimen/drawable_padding</item>
        <item name="android:textColor">@color/colorSecondaryText</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <!-- Contact and More info title style for place details list item-->
    <style name="contact_and_more_info_title_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">@dimen/place_details_list_item_extra_margin</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textSize">@dimen/secondary_title_text_size</item>
    </style>

</resources>
